# MPA_SSM_and_ScapePlot_C4

 ``` 
py -3.10 -m venv .venv
.\.venv\Scripts\Activate.ps1
 ``` 

 ``` 
python -m pip install --upgrade pip
python -m pip install "numpy<2" "librosa==0.10.0.post2" "numba<0.60" "soundfile" "audioread"
 ``` 

 ``` 
python -m pip install notebook ipykernel
python -m ipykernel install --user --name fmp-venv --display-name "Python (fmp-venv)"
 ``` 

 ``` 
jupyter notebook
 ``` 


Things we need sound from:
- Birdsounds (internet) (xeno-canto.org)
- Lively Street (right around the corner)
- Water Splashing
- Motor sound
- The way to the university